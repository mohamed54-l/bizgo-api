export class PaymentsResource {
    constructor(client) {
        this.client = client;
    }
    /**
     * Initialize a payment and get a checkout URL.
     * POST /v1/payments/initialize
     */
    initialize(params) {
        return this.client._post('payments/initialize', params);
    }
    /**
     * Retrieve the full details of a payment transaction.
     * GET /v1/payments/{id}
     */
    retrieve(id) {
        return this.client._get(`payments/${id}`);
    }
    /**
     * Verify a payment transaction.
     * Use this to confirm the status before crediting/debiting your customer.
     * GET /v1/payments/{id}/verify
     */
    verify(id) {
        return this.client._get(`payments/${id}/verify`);
    }
}
//# sourceMappingURL=payments.js.map